1) Install coco and pycma from repositories directly
    - cocoex through `python do.py run-python`
    - pycma through `python setup.py install`
2) Fix scikit-learn imports (they are actually not used)